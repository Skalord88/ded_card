<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog/1.7"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog/1.7 http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-1.7.xsd">
    <changeSet id="005_class_character_spells_known_update" author="oscar">

        <!-- BARD -->
        <update tableName="class_character">
            <column name="spells_known_id" valueNumeric="1" />
        <where>id = 2</where>
        </update>
        <!-- BARD -->
        <!-- CLERIC -->
        <update tableName="class_character">
            <column name="spells_known_id" valueNumeric="4" />
        <where>id = 3</where>
        </update>
        <!-- CLERIC -->
        <!-- DRUID -->
        <update tableName="class_character">
            <column name="spells_known_id" valueNumeric="4" />
        <where>id = 4</where>
        </update>
        <!-- DRUID -->
        <!-- PALADIN -->
        <update tableName="class_character">
            <column name="spells_known_id" valueNumeric="6" />
        <where>id = 7</where>
        </update>
        <!-- PALADIN -->
        <!-- RANGER -->
        <update tableName="class_character">
            <column name="spells_known_id" valueNumeric="6" />
        <where>id = 8</where>
        </update>
        <!-- RANGER -->
        <!-- SORCERER -->
        <update tableName="class_character">
            <column name="spells_known_id" valueNumeric="8" />
        <where>id = 10</where>
        </update>
        <!-- SORCERER -->
        <!-- WIZARD -->
        <update tableName="class_character">
            <column name="spells_known_id" valueNumeric="9" />
        <where>id = 11</where>
        </update>
        <!-- WIZARD -->
        <!-- ASSASSIN -->
        <update tableName="class_character">
            <column name="spells_known_id" valueNumeric="11" />
        <where>id = 15</where>
        </update>
        <!-- ASSASSIN -->

    </changeSet>
</databaseChangeLog>