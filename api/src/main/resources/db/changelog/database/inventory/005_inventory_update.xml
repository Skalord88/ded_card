<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog/1.7"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog/1.7 http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-1.7.xsd">
    <changeSet id="005_inventory_update" author="oscar">

        <update tableName="inventory">
            <!-- Puli -->
            <column name="armor_id" valueNumeric="1001" />
        <where>id
            = 100</where>
        </update>
        <update tableName="inventory">
            <!-- Puli -->
            <column name="shield_id" valueNumeric="1002" />
        <where>id
            = 100</where>
        </update>
        <update tableName="inventory">
            <!-- Puli -->
            <column name="armor_id" valueNumeric="1001" />
        <where>id
            = 100</where>
        </update>
        <update tableName="inventory">
            <!-- Puli -->
            <column name="weapon_id_one" valueNumeric="1003" />
        <where>id
            = 100</where>
        </update>
        <update tableName="inventory">
            <!-- Puli -->
            <column name="weapon_id_two" valueNumeric="1004" />
        <where>id
            = 100</where>
        </update>
        <update tableName="inventory">
            <!-- Puli -->
            <column name="weapon_id_three" valueNumeric="1005" />
        <where>id
            = 100</where>
        </update>
        <update tableName="inventory">
            <!-- Puli -->
            <column name="weapon_id_four" valueNumeric="1006" />
        <where>id
            = 100</where>
        </update>
        <update tableName="inventory">
            <!-- Puli -->
            <column name="weapon_id_five" valueNumeric="2002" />
        <where>id
            = 100</where>
        </update>

    </changeSet>
</databaseChangeLog>